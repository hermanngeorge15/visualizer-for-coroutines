feat: add HTTP endpoint integration tests and event serialization tests

Add comprehensive test coverage for the backend HTTP API:
- SessionRoutes integration tests (full CRUD lifecycle)
- ScenarioRoutes integration tests (scenario listing, execution)
- Event serialization round-trip tests for all coroutine event types

Also fix compilation issues from event package refactoring:
- Move flow event imports to events.flow subpackage
- Remove duplicate root-level event files
- Stub out broken VizEventMain-dependent routes
- Add ktor-client-content-negotiation test dependency
